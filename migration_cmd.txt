Connection :- postgres://username:password@hostname:port/database_name?sslmode=disable


Create Migration:-    goose -dir ./migrations create users sql

Edit Migration File:-

 -- +goose Up
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    password VARCHAR(255) NOT NULL
);

-- +goose Down
DROP TABLE users;


Migrate:-              goose up postgres://postgres:root@localhost:5433/postgres?sslmode=disable migrations 

